import React from 'react';
import ReactDOM from 'react-dom';
import { wsFetch } from '../websocket';

class CommandModuleTabs extends React.Component<any, {}> {

    componentDidMount() {
    }

    render() {
        return (
            <div id="command-module-tabs">
                tabs
            </div>
        );
    }
}

export default CommandModuleTabs;